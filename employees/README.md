# Employees

Здесь появляются папки onboarding-пакетов сотрудников.

Синтетические демо-пакеты имеют префикс `demo_`. Реальные локальные результаты с датой в имени (`20*/`) игнорируются в `.gitignore`, чтобы персональные данные не попали в публичный пакет по ошибке.
